plugins {
	id "com.yilin.module"
}

description = "YiLin Reactive Annotation Processor Generation spring.factories and Java SPI file"

dependencies {
	testImplementation("org.springframework.boot:spring-boot-starter-web")
	testImplementation(project(":yilin-reactive-tests"))
}